export type Product = {
    id: number;
    name: string;
    price: number;
    imageUrl: string;
    color: Array<Array<string>>;
    fullDescription: string;
    shortDescription: string;
    sizes: Object;

};